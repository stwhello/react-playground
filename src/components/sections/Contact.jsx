export default function Contact() {
  return (
    <section className="section" id="contact">
      <span className="eyebrow">#contact</span>
      <h2>Contact — placeholder</h2>
      <p>This is where the Globe / particle field + animated button will live.</p>
    </section>
  );
}
